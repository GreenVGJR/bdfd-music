$nomention

$var[token;$getVar[botToken]]
$var[colorEmbed;a09fff]
$var[configMusic;$getServerVar[configMusic]]
$jsonParse[$var[configMusic]]

$botTyping

$try
$httpGet[$getVar[apiDomain]/music/where?token=$var[token]&guildId=$guildID&authorId=$authorID]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;author]==]
You must be on voice channel.
$stop
$endif

$if[$and[$httpResult[data;data;0;client]!=;$httpResult[data;data;0;sameChannel]!=true]]
You must be on same voice channel in <#$httpResult[data;data;0;client;id]>.
$stop
$endif

$try
$httpGet[$getVar[apiDomain]/music/back?token=$var[token]&guildId=$guildID]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;data;action]==back]
$if[$json[messagestatus;0]!=true]
$if[$httpResult[data;data;currentTrack;is247]==true]
$author[Now Playing - 24/7]
$else
$author[Now Playing]
$endif
$authorIcon[$userAvatar[$botID]?size=2048]
$title[$httpResult[data;data;currentTrack;title]]
$embeddedURL[$httpResult[data;data;currentTrack;url]]
$color[$var[colorEmbed]]
$footer[$displayName - $httpResult[data;data;currentTrack;source]]
$footerIcon[$authorAvatar?size=2048]
$thumbnail[$httpResult[data;data;currentTrack;thumbnail]]
$addField[Duration;$httpResult[data;data;currentTrack;duration];true]
$addField[Loop;$httpResult[data;data;currentTrack;loop];true]
$addField[Volume;$httpResult[data;data;currentTrack;volume]%;true]
$endif
$else
$if[$httpResult[data;type;alt]==inactive_queue]
Nothing is playing.
$elseif[$httpResult[data;type;alt]==empty_queue]
Nothing to skip.
$else
$httpResult[data;message]
$httpResult[error;message]
$endif

$endif