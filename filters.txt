$nomention

$var[token;$getVar[botToken]]
$var[colorEmbed;a09fff]

$botTyping

$try
$httpGet[$getVar[apiDomain]/music/where?token=$var[token]&guildId=$guildID&authorId=$authorID]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;author]==]
You must be on voice channel.
$stop
$endif

$if[$and[$httpResult[data;data;0;client]!=;$httpResult[data;data;0;sameChannel]!=true]]
You must be on same voice channel in <#$httpResult[data;data;0;client;id]>.
$stop
$endif

$if[$noMentionMessage[1]==]
$try
$httpGet[$getVar[apiDomain]/music/filter?token=$var[token]&guildId=$guildID]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;type;primary]==final]
$color[$var[colorEmbed]]
$author[Available Filters]
$description[`$httpResult[data;data;presetFilters;string]`]
$if[$httpResult[data;data;enabledFilters;string]!=]
$addField[Enabled;$httpResult[data;data;enabledFilters;string];false]
$endif
$addField[Usage;`$commandTrigger <filter>` - Apply/disable a filter\n`$commandTrigger reset` - Clear all filters;false]
$else
$httpResult[data;message]
$httpResult[error;message]
$endif
$else
$try
$httpGet[$getVar[apiDomain]/music/filter?token=$var[token]&guildId=$guildID&filter=$noMentionMessage[1]]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;type;primary]==final]
$if[$httpResult[data;data;action]==filters_reset]
Reset all filters.
$elseif[$httpResult[data;data;action]==filter_applied]
Applied: `$httpResult[data;data;filter]` - $httpResult[data;data;description]
$else
Disabled: `$httpResult[data;data;filter]` - $httpResult[data;data;description]
$endif
$else
$httpResult[data;message]
$httpResult[error;message]
$endif
$endif