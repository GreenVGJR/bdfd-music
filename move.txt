$nomention

$onlyIf[$and[$isNumber[$noMentionMessage[1]]==true;$isNumber[$noMentionMessage[2]]==true];Usage: `$commandTrigger <from> <to>`]

$var[token;$getVar[botToken]]
$var[from;$sub[$noMentionMessage[1];1]]
$var[to;$sub[$noMentionMessage[2];1]]
$botTyping

$try
$httpGet[$getVar[apiDomain]/music/where?token=$var[token]&guildId=$guildID&authorId=$authorID]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;author]==]
You must be on voice channel.
$stop
$endif

$if[$and[$httpResult[data;data;0;client]!=;$httpResult[data;data;0;sameChannel]!=true]]
You must be on same voice channel in <#$httpResult[data;data;0;client;id]>.
$stop
$endif

$try
$httpGet[$getVar[apiDomain]/music/move?token=$var[token]&guildId=$guildID&from=$var[from]&to=$var[to]]
$catch
Seems like this cannot be process.
$error[message]
$stop
$endtry

$if[$httpResult[data;type;primary]==final]
Moved: `$httpResult[data;data;fromTrack;title]` → `$httpResult[data;data;toTrack;title]`
$else
$httpResult[data;message]
$httpResult[error;message]
$endif